<Window x:Class="WpfApp1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <!-- Margin="10 20 30 40" -->
        <!-- Padding="5 10 15 20" -->
        <Button Grid.Column="0"
                Grid.Row="0"
                Content="show processes"
                FontSize="16"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Padding="20 5"
                Click="Button_OnClick">
        </Button>
        <TextBox Grid.Row="0" Grid.Column="1" x:Name="Process_name" Width="120" Height="30" HorizontalAlignment="Right" VerticalAlignment="Top"/>
        <TextBlock Grid.Row="0" Grid.Column="1" Width="120" Height="30" HorizontalAlignment="Left" VerticalAlignment="Top" FontSize="18">Process Name:</TextBlock>
        <TextBox Grid.Row="0" Grid.Column="1" x:Name="Process_priority" Width="120" Height="30" HorizontalAlignment="Right" VerticalAlignment="Center"/>
        <TextBlock Grid.Row="0" Grid.Column="1" Padding="55,2" Width="120" Height="30" HorizontalAlignment="Left" VerticalAlignment="Center" FontSize="18">Priority:</TextBlock>
        <Button Grid.Row="0" Grid.Column="1" Content="Submit" Click="SubmitButton_Click" FontSize="16" Width="80" Height="30" VerticalAlignment="Bottom"/>
        <TextBox Grid.Row="0" Grid.Column="2" x:Name="Stop" Width="120" Height="30" HorizontalAlignment="Right" VerticalAlignment="Top"/>
        <TextBlock Grid.Row="0" Grid.Column="2" Width="120" Height="30" HorizontalAlignment="Left" VerticalAlignment="Top" FontSize="17">Process To Stop:</TextBlock>
        <Button Grid.Row="0" Grid.Column="2" Content="Submit" Click="Button_OnClick1"
                FontSize="16" Width="80" Height="30" VerticalAlignment="Center"/>
        <ListBox x:Name="ProcessIds" ItemsSource="{Binding Id}" Grid.Row="1" Grid.Column="0" Grid.RowSpan="2"></ListBox>
        <ListBox x:Name="ProcessNames" ItemsSource="{Binding Name}" Grid.Row="1" Grid.Column="1" Grid.RowSpan="2"></ListBox>
    </Grid>
</Window>
